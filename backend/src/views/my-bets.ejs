<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>GamblinHub - My Bets</title>
  <link rel="icon" type="image/jpeg" href="/imagenes/favicon.png">
  <link rel="stylesheet" href="/css/gamble.css">
  <style>
    .bet-list {
      max-width: 800px;
      margin: 2rem auto;
      padding: 2rem;
      background: #141421;
      border: 1px solid #2a2a3a;
      border-radius: 12px;
      color: #fff;
    }

    .bet-item {
      padding: 1rem;
      border-bottom: 1px solid #333;
    }

    .bet-item:last-child {
      border-bottom: none;
    }

    .bet-title {
      font-size: 1.2rem;
      color: #00ffcc;
    }

    .bet-details {
      color: #ccc;
      font-size: .95rem;
      margin-top: .5rem;
    }

    .back-btn {
      position: absolute;
      top: 20px;
      left: 20px;
      background: #444;
      padding: 10px 18px;
      border-radius: 6px;
      color: white;
      text-decoration: none;
      border: 1px solid #666;
    }
  </style>
</head>

<body>
  <a class="back-btn" href="/gamble">⬅ Back to Gambling</a>
  <main>
    <div class="bet-list">
      <h1 style="text-align:center; margin-bottom:1rem; color:#00ffcc;">
        My Bets
      </h1>

      <% if (!groupedBets || Object.keys(groupedBets).length===0) { %>
        <p style="text-align:center; color:#aaa;">You have no bets yet.</p>
        <% } %>


          <% Object.keys(groupedBets).forEach(fixtureId=> { %>

            <div class="bet-item" style="margin-bottom: 2rem;">

              <% const mainBet=groupedBets[fixtureId][0]; %>

                <h2 class="bet-title" style="color:#00ffcc;">
                  <%= mainBet.homeTeam %> vs <%= mainBet.awayTeam %>
                </h2>

                <% groupedBets[fixtureId].forEach(bet=> { %>
                  <div class="bet-details" style="padding:1rem; border-bottom:1px solid #333;">
                    <p><strong>Selección:</strong>
                      <%= bet.selection.toUpperCase() %>
                    </p>
                    <p><strong>Stake:</strong> €<%= bet.amount %>
                    </p>
                    <p><strong>Cuota:</strong>
                      <%= bet.odd %>
                    </p>
                    <p><strong>Potencial:</strong> €<%= bet.potentialWin.toFixed(2) %>
                    </p>
                    <p><strong>Status:</strong>
                      <%= bet.status %>
                    </p>
                    <p><strong>Fecha:</strong>
                      <%= new Date(bet.createdAt).toLocaleString() %>
                    </p>
                  </div>
                  <% }) %>
            </div>
            <% }) %>
    </div>
  </main>
</body>

</html>