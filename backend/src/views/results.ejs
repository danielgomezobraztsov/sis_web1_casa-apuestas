<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Resultados Recientes</title>

    <link rel="stylesheet" href="/css/results.css">

    <style>
        h1 {
            color: #b066ff;
            text-align: center;
            margin-top: 40px;
        }

        h2 {
            color: #c084fc;
            margin-top: 40px;
            text-align: center;
        }

        table {
            width: 85%;
            margin: 20px auto;
            border-collapse: collapse;
            background: #1a0020;
            color: #fff;
            border-radius: 10px;
            overflow: hidden;
        }

        th {
            background: #8a2be2;
            padding: 12px;
            text-align: center;
        }

        td {
            padding: 10px;
            text-align: center;
            border-bottom: 1px solid #32103f;
        }

        tr:hover {
            background: #2d003a;
        }

        .team {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 6px;
        }

        .team img {
            height: 22px;
        }

        p.notice {
            text-align: center;
            color: #ccc;
        }
    </style>
</head>

<body>

<h1>Resultados Recientes</h1>
<p class="notice">Partidos finalizados del día actual</p>

<%
    const leagueIds = Object.keys(results);
    if (leagueIds.length === 0) {
%>

    <p class="notice">⚠️ No hay partidos finalizados todavía hoy.</p>

<% } %>

<% leagueIds.forEach(leagueId => { %>

    <h2><%= results[leagueId].leagueName %></h2>

    <% if (results[leagueId].matches.length === 0) { %>

        <p class="notice">No hubo partidos finalizados en esta liga.</p>

    <% } else { %>

        <table>
            <thead>
                <tr>
                    <th>Local</th>
                    <th>Resultado</th>
                    <th>Visitante</th>
                </tr>
            </thead>

            <tbody>
                <% results[leagueId].matches.forEach(match => { %>

                    <tr>
                        <td>
                            <div class="team">
                                <img src="<%= match.teams.home.logo %>" alt="">
                                <span><%= match.teams.home.name %></span>
                            </div>
                        </td>

                        <td><%= match.goals.home %> - <%= match.goals.away %></td>

                        <td>
                            <div class="team">
                                <span><%= match.teams.away.name %></span>
                                <img src="<%= match.teams.away.logo %>" alt="">
                            </div>
                        </td>
                    </tr>

                <% }) %>
            </tbody>
        </table>

    <% } %>

<% }) %>

</body>
</html>
